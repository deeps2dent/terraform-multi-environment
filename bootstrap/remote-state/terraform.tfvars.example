aws_region   = "us-east-2"
project_name = "terraform-multi-environment"

# Leave this commented so Terraform automatically creates a globally
# unique bucket name using your AWS account ID and region.
# state_bucket_name = "your-globally-unique-terraform-state-bucket"

lock_table_name = "terraform-multi-environment-locks"

tags = {
  Owner       = "Gurdeep"
  Environment = "shared"
}